# CLAUDE.md

This file is a compatibility entrypoint for Claude Code.

Read and follow `GOATED.md` first. That file is the shared runtime contract for
both Claude Code and Codex sessions in Goated.

After `GOATED.md`, continue with:
- `GOATED_CLI_README.md`
- `self/CLAUDE.md`
- `self/AGENTS.md` if it exists

Guide for building local CLI tools is in `TOOLS.md`.
Agent credentials are file-backed in `creds/*.txt` and managed via `./goat`.
